#!/usr/bin/env bash
export PIPER_LENGTH_SCALE="${PIPER_LENGTH_SCALE:-0.96}"
export PIPER_SENTENCE_SILENCE="${PIPER_SENTENCE_SILENCE:-0.14}"
export PIPER_VOLUME_PULSE="${PIPER_VOLUME_PULSE:-52000}"
exec /home/pi/.local/bin/mdm-piper-tts "$@"
